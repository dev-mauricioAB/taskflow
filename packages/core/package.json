{
  "name": "@repo/core",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "@repo/shared": "*",
    "@vitest/coverage-istanbul": "^3.2.4",
    "typescript": "^5.4.0",
    "vitest": "^3.2.4"
  }
}
