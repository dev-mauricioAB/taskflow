{
  "name": "@repo/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"
  },
  "devDependencies": {
    "@repo/core": "*",
    "typescript": "5.9.2",
    "prisma": "^5.21.1",
    "@prisma/client": "^5.21.1"
  }
}
