export const DEFAULT_PAGE_LIMIT = 20;
export const DEFAULT_OFFSET = 0;
export const DEFAULT_SORT_DIR = "desc" as const;

export const DEFAULT_SORT_BY_TASK = "createdAt" as const;

export const DEFAULT_ACTIVITY_LIMIT = 5;
